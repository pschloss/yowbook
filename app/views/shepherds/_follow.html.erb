<%= form_for(current_shepherd.active_relationships.build, remote: true) do |f| %>
	<div><%= hidden_field_tag :followed_id, @shepherd.id %></div>
	<%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
