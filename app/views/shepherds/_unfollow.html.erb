<%= form_for(current_shepherd.active_relationships.find_by(followed_id: @shepherd.id),
																		html: { method: :delete },
																		remote: true) do |f| %>
	<%= f.submit "Unfollow", class: "btn" %>
<% end %>
