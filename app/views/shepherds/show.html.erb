<% provide(:title, "#{@shepherd.name} (@#{@shepherd.username})") %>
<div class="row">
	<aside class="col-md-4">
		<section class="shepherd_info">
			<%= render 'shepherd_info' %>
		</section>
		<section class="stats">
			<%= render 'shared/stats' %>
		</section>
		<%= render 'animal_form' %>
	</aside>

	<div class="col-md-5">
		<%= render 'follow_form' %>
		<% if @shepherd.animals.any? %>
			<ol class="animals">
				<%= render @shepherd.animals %>
			</ol>
			<%= will_paginate @animals %>
		<% end %>
	</div>
	<div class="col-md-3">
		<h3>Spacer</h3>
	</div>
</div>
